{
  "name": "dramabox-bot",
  "version": "1.2.0",
  "description": "Telegram bot for DramaBox video search — KevLang-Pure v2.1.0, Bun, Turso, auto-deleting episodes",
  "type": "module",
  "scripts": {
    "build": "kev build src/db.kev && kev build src/seed.kev && kev build src/config.kev && kev build src/api.kev && kev build src/helpers.kev && kev build src/handlers.kev && kev build src/bot.kev",
    "start": "bun run dist/bot.ts",
    "dev": "kev run src/bot.kev",
    "seed": "kev run src/seed.kev",
    "check": "kev check src/bot.kev"
  },
  "dependencies": {
    "grammy": "^1.21.1",
    "@libsql/client": "^0.6.0"
  },
  "devDependencies": {
    "@itskevz/kevlang-pure": "2.1.0",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": ["telegram", "bot", "dramabox", "kevlang-pure", "bun", "turso"],
  "license": "MIT"
}
