{
  "name": "dramabox-bot",
  "version": "1.2.1",
  "type": "module",
  "scripts": {
    "build": "kevpure build src/seed.kev && kevpure build src/bot.kev",
    "start": "bun run src/bot.ts",
    "dev": "kevpure run src/bot.kev",
    "seed": "kevpure run src/seed.kev"
  },
  "dependencies": {
    "grammy": "^1.21.1",
    "@libsql/client": "^0.6.0"
  },
  "devDependencies": {
    "kevlang-pure": "2.1.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
