{
  "name": "truffle-reels-bot",
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "build": "kevpure build src/db.kev && kevpure build src/seed.kev && kevpure build src/config.kev && kevpure build src/api.kev && kevpure build src/helpers.kev && kevpure build src/handlers.kev && kevpure build src/bot.kev",
    "start": "bun run src/bot.ts",
    "dev": "kevpure run src/bot.kev",
    "seed": "kevpure run src/seed.kev"
  },
  "dependencies": {
    "grammy": "^1.21.1",
    "@libsql/client": "^0.6.0"
  },
  "devDependencies": {
    "kevlang-pure": "2.1.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
